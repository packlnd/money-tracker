.control-group
  - Category.all.each do |category|
    %label.checkbox.inline
      .span{:class => "#{category.css}"}
        %input{:type => 'checkbox', :checked => 'yes', :name => "category[#{category.id}]"} #{category.name}
.control-group
  - @user_transactions = Transaction.order(Sequel.desc(:timestamp)).where(:owner => $user)
  Från
  %input.input-small{type: 'text', name: 'date_from', value: "#{Transaction.last.to_s}"}
  till
  %input.input-small{type: 'text', name: 'date_to', value: "#{Transaction.first.to_s}"}
